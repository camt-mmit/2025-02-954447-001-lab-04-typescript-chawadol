#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../copy-and-watch/bin/copy-and-watch" "$@"
  ret=$?
else 
  node  "$basedir/../copy-and-watch/bin/copy-and-watch" "$@"
  ret=$?
fi
exit $ret
